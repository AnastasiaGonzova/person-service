server:
  port: 8090
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=medical_schema
    username: postgres
    password: 31121998
    driver-class-name: org.postgresql.Driver
  liquibase:
    change-log: classpath:/db/changelog.xml
    enabled: true
  jpa:
    hibernate:
      ddl-auto: none